# DishGal recipe expansion

These files are merged into the base recipe library during the GitHub Pages build. The merge validator fails the build on duplicate slugs, normalized titles, or recipe image URLs.
